/*import 'package:alpha_gymnastic_center/domain/entities/course.dart';

class Search {
  List<Course> courses;

  Search({required this.courses});

  List<Course> searchByTitle(String title) {
    return courses
        .where((course) =>
            course.title.toLowerCase().contains(title.toLowerCase()))
        .toList();
  }
}*/
